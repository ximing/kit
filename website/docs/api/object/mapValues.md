---
id: mapValues
title: mapValues
description: 'Creates an object with the same keys as object and values generated by running each own enumerable string keyed property through iteratee'
---

# `mapValues`

Creates an object with the same keys as object and values generated by running each own enumerable string keyed property through iteratee

## Parameters

| Parameter  | Type  | Description                          |
| ---------- | ----- | ------------------------------------ |
| `obj`      | `any` | - The object to iterate over         |
| `iteratee` | `any` | - The function invoked per iteration |

## Returns

- **Type**: `any`
- **Description**: Returns the new mapped object

## Examples

```typescript
* mapValues({ a: 1, b: 2 }, (value) => value * 2); // { a: 2, b: 4 }
```

## Interactive Example

```tsx live
function mapValuesExample() {
  return (
    <div style={{ padding: '20px', background: '#f5f5f5', borderRadius: '8px' }}>
      <h4>`mapValues` Example</h4>
      <p>
        Creates an object with the same keys as object and values generated by running each own enumerable string keyed
        property through iteratee
      </p>
    </div>
  );
}
```
